#!/usr/bin/env bash

. /etc/ha-mqtt-broker.conf
TP_REMOTE=steam/remote/${MQTT_TOPIC_AFFIX_MACHINE}
${MQTT_BIN_PUB} -h $MQTT_HOST -u $MQTT_USER -P $MQTT_PASS -t $TP_REMOTE -m screenFullHD
pa-default-sink hdmi
